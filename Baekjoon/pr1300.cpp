#include <iostream>
#include <algorithm>
#include <vector>
typedef long long ll;
using namespace std;

int n, k;
vector<int> arr;

int main() {
	ios_base::sync_with_stdio(0);
	cin.tie(0);
	cin >> n >> k;

}